# Results

The analytical pipeline produces result tables, figures, validation records, and findings in stage-specific subdirectories. The public repository tracks only the 11 validated result CSVs required by the dashboard; other reproducible generated outputs remain ignored to keep the repository focused.

The tracked historical CSV in `data/processed/` is the dashboard's twelfth data source. See `src/dashboard_data.py` for the authoritative source list.
