# Tests

`test_dashboard_data.py` contains focused standard-library unit tests for the dashboard's read-only data layer, including schema errors, UTC parsing, 24-hour selection, missing-value preservation, and CSV downloads.
