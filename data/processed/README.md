# Processed Data

`eia_ciso_hourly_2022_2024.csv` is the validated 26,304-row historical table used by the public dashboard. It is intentionally tracked so a fresh clone can explore history without downloading from EIA.

The larger feature master and sample outputs are generated locally by the pipeline and remain ignored. Missing source measurements and reported negative solar values are preserved rather than filled or clipped.
