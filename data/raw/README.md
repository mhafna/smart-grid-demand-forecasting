# Raw Data

Raw EIA API JSON responses are created locally by the acquisition scripts and are intentionally ignored. They are not needed to run the dashboard from a clone. Set `EIA_API_KEY` in the shell and follow the root README only when reproducing the full pipeline.
