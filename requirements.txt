# No external package is needed for src/fetch_eia_sample.py.
# It uses Python's standard library to read EIA_API_KEY, make one HTTPS request,
# validate the JSON response, and save the raw file.

# numpy provides numerical arrays and metric calculations throughout the analysis.
numpy

# pandas is used for tabular data preparation and time-series analysis.
pandas

# matplotlib is used to create the reproducible EDA charts saved as PNG files.
matplotlib

# scikit-learn provides the leakage-safe scaling pipeline and Linear Regression.
scikit-learn

# joblib saves and reloads fitted model artifacts during full pipeline reproduction.
joblib

# xgboost provides the gradient-boosted tree regressor used in one-step modelling.
xgboost

# streamlit provides the lightweight interactive dashboard UI.
streamlit

# plotly provides responsive, accessible charts inside the Streamlit dashboard.
plotly
